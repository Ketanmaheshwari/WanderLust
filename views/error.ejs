<% layout("layouts/boilerplate") %> 
<!-- 
    Extends the base layout named 'boilerplate' 
    (likely includes the head, navbar, and basic HTML structure)
-->

<div class="row mt-3">
    <!-- Bootstrap row with margin-top -->
    
    <div class="alert alert-danger col-6 offset-3" role="alert">
        <!-- Centered alert box (6 columns wide, offset by 3 to center) -->
        <!-- Uses Bootstrap 'alert-danger' class for red background -->

        <p class="alert-heading"><%= message %></p>  
        <!-- Display the dynamic error message passed from the server -->
        
    </div>
</div>
